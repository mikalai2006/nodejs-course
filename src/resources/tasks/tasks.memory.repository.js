const getAll = async () => {
  // TODO: mock implementation. should be replaced during task development
  return [
    {
      id: 'string',
      title: 'string',
      order: 0,
      description: 'string',
      userId: 'string'
    }
  ];
};

module.exports = { getAll };
